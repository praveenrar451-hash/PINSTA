<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>@<%= profileUser.username %> • PINSTA</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <nav class="navbar">
        <h2>@<%= profileUser.username %></h2>
        <a href="/chat" style="font-size: 20px; text-decoration: none;" title="Chats">💬</a>
    </nav>

    <div class="container" style="max-width: 600px;">
        <div class="profile-header">
            <div class="profile-avatar">👤</div>
            <div class="profile-info">
                <h3>@<%= profileUser.username %></h3>
                <div class="profile-stats">
                    <span><b><%= posts.length %></b> posts</span>
                    <span onclick="openUserList('followers')" style="cursor: pointer;"><b><%= profileUser.followers.length %></b> followers</span>
                    <span onclick="openUserList('following')" style="cursor: pointer;"><b><%= profileUser.following.length %></b> following</span>
                </div>
                <% if (loggedInUser.username !== profileUser.username) { %>
                    <form action="/follow/<%= profileUser.username %>" method="POST">
                        <button type="submit" class="follow-btn">
                            <%= profileUser.followers.includes(loggedInUser.username) ? 'Following' : 'Follow' %>
                        </button>
                    </form>
                <% } %>
            </div>
        </div>

        <div class="profile-grid">
            <% if (posts.length === 0) { %>
                <p class="no-posts" style="grid-column: span 3;">No posts yet.</p>
            <% } else { %>
                <% posts.forEach(post => { %>
                    <div class="grid-item">
                        <% if (post.image_url) { %>
                            <img src="<%= post.image_url %>" alt="Post">
                        <% } %>
                    </div>
                <% }) %>
            <% } %>
        </div>
    </div>

    <!-- Followers / Following List Modal -->
    <div id="userListModal" class="user-list-modal">
        <div class="user-list-content">
            <span class="close-modal" onclick="closeUserList()">&times;</span>
            <h3 id="userListTitle" style="margin-bottom: 15px; font-size: 16px; border-bottom: 1px solid #333; padding-bottom: 8px;"></h3>
            <div id="userListContainer"></div>
        </div>
    </div>

    <!-- Post Upload Modal -->
    <div id="postModal" class="modal">
        <div class="modal-content">
            <span class="close-modal" onclick="closeModal()">&times;</span>
            <h3 style="margin-bottom: 15px; font-size: 18px;">Create New Post</h3>
            <form action="/post" method="POST" enctype="multipart/form-data">
                <input type="file" name="image" accept="image/*" required style="margin-bottom: 12px; color: #fff;">
                <input type="text" name="content" placeholder="Write a caption..." required>
                <button type="submit">Share Post</button>
            </form>
        </div>
    </div>

    <!-- Search Modal Overlay -->
    <div id="searchModal" class="search-modal">
        <div class="search-header">
            <input type="text" id="searchInput" placeholder="Search username..." onkeyup="performSearch()">
            <button onclick="closeSearch()" style="background:none; border:none; color:#fff; font-size:16px;">Cancel</button>
        </div>
        <div id="searchResults" class="search-results-list"></div>
    </div>

    <!-- Instagram Bottom Navigation Bar -->
    <div class="bottom-nav">
        <a href="/feed" class="nav-item">🏠</a>
        <button class="nav-item" onclick="openSearch()">🔍</button>
        <button class="nav-item" onclick="openModal()">➕</button>
        <a href="/chat" class="nav-item">💬</a>
        <a href="/profile/<%= loggedInUser.username %>" class="nav-item active">👤</a>
    </div>

    <script>
        const followersData = <%- JSON.stringify(profileUser.followers) %>;
        const followingData = <%- JSON.stringify(profileUser.following) %>;

        function openUserList(type) {
            let title = document.getElementById('userListTitle');
            let container = document.getElementById('userListContainer');
            let list = type === 'followers' ? followersData : followingData;
            
            title.innerText = type === 'followers' ? 'Followers' : 'Following';
            let html = '';
            if (list.length === 0) {
                html = '<p style="color: #888; font-size: 13px;">No users found.</p>';
            } else {
                list.forEach(username => {
                    html += `<a href="/profile/${username}" class="user-search-item"><b>@${username}</b></a>`;
                });
            }
            container.innerHTML = html;
            document.getElementById('userListModal').style.display = 'flex';
        }

        function closeUserList() {
            document.getElementById('userListModal').style.display = 'none';
        }

        function openModal() { document.getElementById('postModal').style.display = 'flex'; }
        function closeModal() { document.getElementById('postModal').style.display = 'none'; }
        
        function openSearch() { document.getElementById('searchModal').style.display = 'flex'; document.getElementById('searchInput').focus(); }
        function closeSearch() { document.getElementById('searchModal').style.display = 'none'; }

        function performSearch() {
            let query = document.getElementById('searchInput').value;
            if(query.trim() === '') {
                document.getElementById('searchResults').innerHTML = '';
                return;
            }
            fetch('/search?q=' + query)
            .then(res => res.json())
            .then(users => {
                let html = '';
                users.forEach(u => {
                    html += `<a href="/profile/${u.username}" class="user-search-item"><b>@${u.username}</b></a>`;
                });
                document.getElementById('searchResults').innerHTML = html;
            });
        }
    </script>
</body>
</html>
